TIMEZONE=Europe/Stockholm
ACME_EMAIL=
CF_DNS_API_TOKEN=

# Generate with: htpasswd -nbB <user> <password>
# Quote the hash in a literal .env so Compose does not expand $; paste it
# unquoted into the Dockhand secret. See README "Prepare Traefik State".
TRAEFIK_BASIC_AUTH_USERS=''
