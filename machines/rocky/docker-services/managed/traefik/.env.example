TIMEZONE=Europe/Stockholm
ACME_EMAIL=
CF_DNS_API_TOKEN=

# Generate with: htpasswd -nbB <user> <password>
# Keep the single quotes so Docker Compose does not expand $ in the hash.
TRAEFIK_BASIC_AUTH_USERS=''
