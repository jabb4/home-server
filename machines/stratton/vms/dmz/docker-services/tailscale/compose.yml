# Verified working with rootfull docker (sudo docker compose up -d)

services:
  tailscale:
    container_name: tailscale
    image: tailscale/tailscale:v1.96.5@sha256:dbeff02d2337344b351afac203427218c4d0a06c43fc10a865184063498472a6
    restart: always

    # Filesystem
    volumes:
      - ./state:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    
    # Security
    cap_add:
      - NET_ADMIN
      - NET_RAW
    security_opt:
      - no-new-privileges:true  # forbid privilege escalation
    
    # Environment
    environment:
      - TS_AUTHKEY=${TS_AUTHKEY}
      - "TS_EXTRA_ARGS=--advertise-exit-node --advertise-routes=192.168.0.0/16"
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_USERSPACE=false
    
    hostname: homeserver
