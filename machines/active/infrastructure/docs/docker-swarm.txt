#### Not in use




This machine is the swarm manager.

To init the docker swarm manager type: docker swarm init --advertise-addr <MANAGER_IP>
Create a network with driver: overlay

Then when manager is setup go to all vm and join them as workers.

Join as worker with this command: docker swarm join --token <worker-token> <MANAGER_IP>:2377
set network to the big overlay network
